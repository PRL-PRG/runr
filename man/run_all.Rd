% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run-package-code.R
\name{run_all}
\alias{run_all}
\title{Run every R file under a directory}
\usage{
run_all(
  path,
  output_dir = getwd(),
  run_dir = tempfile(),
  filter = NULL,
  wrap_code_fun = NULL,
  clean = TRUE,
  quiet = TRUE,
  skip_if_out_exists = TRUE
)
}
\arguments{
\item{path}{directory holding the R files to run.}

\item{output_dir}{directory to write the per-file \code{.out} files to.}

\item{run_dir}{scratch directory to copy the code into before running. Pass
\code{path} to run in place.}

\item{filter}{regexp on file names, or \code{NULL} to run everything.}

\item{wrap_code_fun}{\verb{function(code)} returning the new file contents, applied
to each file just before it runs.}

\item{clean}{remove \code{run_dir} afterwards.}

\item{quiet}{do not report progress.}

\item{skip_if_out_exists}{treat a file whose \code{.out} already exists as done.}
}
\value{
A data frame with one row per file and the columns \code{file}, \code{out_file},
\code{exitval}, \code{time} and \code{error} (the message if the file could not be run at
all, \code{NA} otherwise).
}
\description{
Runs each \code{.R} file with \code{\link[=run_one]{run_one()}}. The individual files under a \verb{testthat/}
directory are skipped, because the extracted testthat drivers run them. By
default the code is copied into a scratch directory first, so the corpus is
left untouched even when \code{wrap_code_fun} rewrites files.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-parallel-results.R
\name{read_files}
\alias{read_files}
\title{Read many files, turning failures into values}
\usage{
read_files(
  jobs,
  files,
  readf = read_lines,
  mapf = function(job, x) x,
  mapf_error = function(...) structure(list(...), class = "error"),
  reducef = identity,
  quiet = TRUE
)
}
\arguments{
\item{jobs}{job names, parallel to \code{files}.}

\item{files}{paths to read, parallel to \code{jobs}.}

\item{readf}{function used to read one file.}

\item{mapf}{\verb{function(job, contents)} applied to each successful read.}

\item{mapf_error}{function called with the job, the file and the error
message when a read fails. Defaults to returning a condition object.}

\item{reducef}{function applied to the whole list of results.}

\item{quiet}{do not message about individual read failures.}
}
\value{
The result of \code{reducef} applied to the per-file results, a list named
by \code{files} before reduction.
}
\description{
Bulk reader behind the other \verb{read_parallel_*} functions. Shows a progress
bar, and records a read failure as a value instead of aborting the batch, so
that one unreadable file does not lose the whole run.
}

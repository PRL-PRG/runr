% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run-package-code.R
\name{run_one}
\alias{run_one}
\title{Run a single R file in a fresh R subprocess}
\usage{
run_one(file, out_file, cwd = TRUE, quiet = TRUE, stats = TRUE)
}
\arguments{
\item{file}{path to the R file to run.}

\item{out_file}{where to write the combined stdout and stderr, or \code{NULL} to
let it go to the console.}

\item{cwd}{run from the file's own directory rather than the current one.}

\item{quiet}{do not print the command being run.}

\item{stats}{also report the elapsed time, recovered from the trailing
\code{proc.time()} in the output.}
}
\value{
A one-row data frame with the \code{exitval}, and the elapsed \code{time} when
\code{stats} is \code{TRUE} (\code{NA} if the run failed or the timing could not be read).
}
\description{
Runs \code{file} with the current R build under a fixed environment (\code{LC_ALL=C}, no
browser, no PDF viewer, sources kept) so that runs are comparable across a
corpus.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install-cran-packages.R
\name{install_cran_packages}
\alias{install_cran_packages}
\title{Install CRAN packages into a dedicated library}
\usage{
install_cran_packages(
  packages,
  lib_dir = NULL,
  r_home = R.home(),
  dest_dir = NULL,
  mirror = "https://cloud.r-project.org/",
  force = FALSE,
  dependencies = TRUE,
  check = TRUE,
  install_opts = c("--example", "--install-tests", "--with-keep.source",
    "--no-multiarch"),
  n_cpus = floor(0.9 * parallel::detectCores())
)
}
\arguments{
\item{packages}{names of the packages to install, or \code{NULL} for every package
available from \code{mirror}.}

\item{lib_dir}{library to install into, created if needed. \code{NULL} uses the
default library.}

\item{r_home}{the \code{R_HOME} of the R installation to install with.}

\item{dest_dir}{directory to keep the downloaded sources in, created if
needed.}

\item{mirror}{CRAN mirror to install from.}

\item{force}{install even the packages that are already present.}

\item{dependencies}{passed to \code{\link[utils:install.packages]{utils::install.packages()}}.}

\item{check}{also reinstall packages that are installed but fail to load.}

\item{install_opts}{options passed to \verb{R CMD INSTALL}.}

\item{n_cpus}{number of parallel installation jobs.}
}
\value{
The paths to \code{packages}. This might be a subset of
the requested packages, if some packages failed to install.
}
\description{
Installs the packages that are missing from \code{lib_dir} in a separate R
process, keeping tests and source references so that the installed copies can
be used for experiments.
}

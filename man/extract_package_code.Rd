% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-package-code.R
\name{extract_package_code}
\alias{extract_package_code}
\title{Extract a package's runnable code into standalone R files}
\usage{
extract_package_code(
  pkg,
  pkg_dir = find.package(pkg),
  types = c("examples", "tests", "vignettes", "all"),
  output_dir,
  wrap = NULL,
  filter = NULL,
  split_testthat = FALSE,
  compute_sloc = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{pkg}{name of the package.}

\item{pkg_dir}{the installed package directory to extract from.}

\item{types}{which kinds of code to extract; \code{"all"} selects every kind.}

\item{output_dir}{directory to write the extracted files to, created if
needed.}

\item{wrap}{\code{NULL}, a \verb{function(package, file, type, body)} returning the new
contents of each extracted file, or the path of a template file. A template
is turned into such a function by \code{\link[=wrap_using_template]{wrap_using_template()}}, which substitutes
the \code{.PACKAGE.}, \code{.FILE.}, \code{.TYPE.} and \code{.BODY.} placeholders.}

\item{filter}{regexp on the extracted file names, or \code{NULL} to keep all.}

\item{split_testthat}{emit one driver per \code{test_*.R} file instead of a single
\code{testthat.R}, so a crashing test file does not take the suite down with it
and each file gets its own exit code.}

\item{compute_sloc}{also count lines of code, which requires \code{cloc}.}

\item{quiet}{do not report progress.}
}
\value{
A tibble with one row per extracted file: \code{file}, relative to
\code{output_dir}, and \code{type}. When \code{compute_sloc} is \code{TRUE} the \code{blank},
\code{comment} and \code{code} counts of the code the file came from are included, so
for a testthat driver they describe the test file it runs rather than the
driver itself.
}
\description{
A package's executable code is not directly runnable: examples live inside
\code{Rd} files, vignettes inside \code{Rmd}, and tests behind a \code{testthat.R} driver.
This writes all of it out as plain \code{.R} files under \verb{output_dir/<type>/}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run-r-file.R
\name{run_r_file}
\alias{run_r_file}
\title{Run an R file with a specific R build}
\usage{
run_r_file(
  file,
  timeout,
  r_envir = c(R_TESTS = "", R_BROWSER = "false", R_PDFVIEWER = "false"),
  r_args = c("--no-save", "--quiet", "--no-readline"),
  r_home = R.home(),
  lib_path = NULL,
  keep_output = c("always", "never", "on_error"),
  quiet = TRUE
)
}
\arguments{
\item{file}{path to the R file to run.}

\item{timeout}{kill the process after this many seconds.}

\item{r_envir}{named character vector of environment variables to set.}

\item{r_args}{command line arguments passed to R.}

\item{r_home}{the \code{R_HOME} of the R installation to run.}

\item{lib_path}{library to run against, exported as \code{R_LIBS}, or \code{NULL} to
inherit the default.}

\item{keep_output}{whether to return the captured output \code{"always"},
\code{"never"}, or only \code{"on_error"}.}

\item{quiet}{unused, kept for symmetry with the other runners.}
}
\value{
A one-row tibble with \code{file}, the exit \code{status}, the \code{elapsed} time in
seconds and the combined stdout/stderr \code{output} (\code{NA} when \code{keep_output}
suppresses it).
}
\description{
Runs \code{file} in a subprocess started from \code{r_home}, from the file's own
directory. Use this rather than \code{\link[=run_one]{run_one()}} when the experiment is about a
particular R build or library; \code{callr} cannot be told which R to use.
}

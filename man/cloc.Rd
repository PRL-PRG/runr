% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{cloc}
\alias{cloc}
\title{Count lines of code with cloc}
\usage{
cloc(path, by_file = FALSE, r_only = FALSE, cloc_bin = "cloc")
}
\arguments{
\item{path}{directory or file to count.}

\item{by_file}{report one row per file rather than one row per language.}

\item{r_only}{keep only the rows counting R code.}

\item{cloc_bin}{name of, or path to, the \code{cloc} executable.}
}
\value{
A tibble with the \code{blank}, \code{comment} and \code{code} counts, identifying
each row by \code{filename} when \code{by_file} is \code{TRUE} and by \code{language} and
\code{files} otherwise (with the queried \code{path} prepended as a column), or \code{NULL}
if \code{cloc} reported nothing.
}
\description{
Runs the external \code{cloc} utility and parses its CSV output.
}

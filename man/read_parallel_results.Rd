% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-parallel-results.R
\name{read_parallel_results}
\alias{read_parallel_results}
\title{Read the results of a GNU parallel run}
\usage{
read_parallel_results(path, stdout = TRUE, stderr = TRUE)
}
\arguments{
\item{path}{the run directory, holding \code{parallel.log} and one subdirectory per
job.}

\item{stdout}{add the captured stdout.}

\item{stderr}{add the captured stderr.}
}
\value{
A tibble of \code{\link[=read_parallel_log]{read_parallel_log()}} joined with \code{job} and \code{path}, plus a
column per requested stream and a matching \verb{_error} column holding the
message when that file could not be read.
}
\description{
Joins the job log with the per-job output directories under \code{path}. The join
is driven by the directories, so a job retried with \code{--retry-failed} — which
appends a second log entry but reuses its directory — contributes a single
row, for the attempt whose output was kept.
}
